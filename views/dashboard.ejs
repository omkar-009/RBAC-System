<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/views/public/style.css">
</head>
<body class="admin-dashboard-page">
    <div class="sidebar">
        <div class="sidebar-title mb-4">
            <i class="bi bi-shield-lock"></i> RBAC System
        </div>
        <nav class="nav flex-column">
            <a class="nav-link active" href="/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a>
            <a class="nav-link" href="/users/profile"><i class="bi bi-person"></i> Profile</a>
            <% if (user.role === 'admin') { %>
            <a class="nav-link" href="/admin/dashboard"><i class="bi bi-people"></i> Admin Panel</a>
            <% } %>
            <div class="dashboard-search">
                <input type="text" placeholder="Search users..." />
            </div>
            <a class="nav-link logout" href="/users/logout"><i class="bi bi-box-arrow-right"></i> Logout</a>
        </nav>
    </div>
    <div class="main-dashboard">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top d-lg-none">
            <div class="container-fluid">
                <a class="navbar-brand" href="/dashboard">
                    <i class="bi bi-shield-lock"></i> RBAC System
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                    <div class="navbar-nav ms-auto">
                        <span class="nav-item nav-link">
                            <i class="bi bi-person-circle"></i> <%= user.name %>
                        </span>
                        <a class="nav-item nav-link" href="/users/logout">
                            <i class="bi bi-box-arrow-right"></i> Logout
                        </a>
                    </div>
                </div>
            </div>
        </nav>
        <div class="dashboard-section dashboard-container">
            <div class="row mb-4">
                <div class="col">
                    <div class="d-flex align-items-center">
                        <div class="me-4 text-center">
                            <div class="position-relative" class="dashboard-photo-wrapper">
                                <% if (user.photo) { %>
                                    <img src="/uploads/<%= user.photo %>" alt="Profile Photo" class="img-thumbnail rounded-circle mb-2" class="dashboard-photo">
                                <% } else { %>
                                    <div class="bg-light text-center rounded-circle mb-2 d-flex align-items-center justify-content-center" class="bg-light rounded-circle dashboard-photo">
                                        <i class="bi bi-person-fill text-muted"></i>
                                    </div>
                                <% } %>
                                <a href="/users/profile" class="btn btn-sm btn-outline-primary d-block mx-auto" class="btn btn-sm btn-outline-primary d-block mx-auto btn-edit-photo">
                                    <i class="bi bi-pencil-square me-1"></i> Edit
                                </a>
                            </div>
                        </div>
                        <div class="flex-grow-1">
                            <h2 class="mb-1">Welcome, <%= user.name %></h2>
                            <p class="lead mb-0 text-muted">Role: <%= user.role %></p>
                            <% if (user.photo) { %>
                                <p class="text-muted small mt-2">
                                    <i class="bi bi-image me-1"></i> <%= user.photo %>
                                </p>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <% if (user.role === 'admin') { %>
                <div class="row g-4">
                    <div class="col-md-6">
                        <div class="card h-100">
                            <div class="card-body">
                                <div class="d-flex align-items-center">
                                    <div class="me-3">
                                        <i class="bi bi-people" class="bi bi-people card-icon"></i>
                                    </div>
                                    <div>
                                        <h5 class="card-title mb-2">Manage Users</h5>
                                        <p class="card-text text-muted mb-3">View and manage all users in the system.</p>
                                        <a href="/admin/dashboard" class="btn btn-primary">
                                            <i class="bi bi-arrow-right"></i> Go to Admin Dashboard
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="card h-100">
                            <div class="card-body">
                                <div class="d-flex align-items-center">
                                    <div class="me-3">
                                        <i class="bi bi-gear" class="bi bi-people card-icon"></i>
                                    </div>
                                    <div>
                                        <h5 class="card-title mb-2">System Settings</h5>
                                        <p class="card-text text-muted mb-3">Configure system settings and preferences.</p>
                                        <a href="#" class="btn btn-outline-primary">
                                            <i class="bi bi-gear"></i> Settings
                                        </a>
                                    </div>
                                </div>
                            </div>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    <% } %>
</div>


